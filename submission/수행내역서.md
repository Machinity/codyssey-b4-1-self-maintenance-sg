# B4-1 요구사항 수행 내역서

> 작성 방법: `<!-- ... -->` 로 표시된 곳을 **본인 VM 에서 실제로 실행한 결과**로 채웁니다.
> 긴 출력은 핵심 줄만 본문에 넣고, 전체는 `evidence/` 파일을 참조합니다.

| 항목 | 내용 |
|---|---|
| 작성자 | <!-- 이름 --> |
| 작성일 | <!-- YYYY-MM-DD --> |
| 시나리오 | 현장 배치 서비스 로봇 온디바이스 Ubuntu 의 보안 설정 및 상태 관제 자동화 |

---

## 0. 실습 환경

| 항목 | 값 |
|---|---|
| OS | Ubuntu 22.04.5 LTS |
| 커널 / 아키텍처 | aarch64 |
| 하이퍼바이저 | UTM |
| 사용 바이너리 | agent-app-linux-arm64 |
| 방화벽 도구 | UFW |
| 증거 | `evidence/00_system.txt` |

---

## 1. SSH 보안 설정

### 1-1. 설정 내용

`/etc/ssh/sshd_config.d/10-agent-hardening.conf`
```
<!-- cat 결과 붙여넣기 -->
```

### 1-2. 수행 명령

```bash
<!-- 백업, 설치, sshd -t, systemctl restart ssh 등 실제 실행 명령 -->
```

### 1-3. 확인 결과

```
<!-- sudo sshd -T | grep -Ei '^(port|permitrootlogin) ' -->
<!-- sudo ss -tulnp | grep sshd -->
```

- 20022 접속: <!-- 성공 -->
- 22 접속: <!-- Connection refused -->
- root 접속: <!-- Permission denied --> (스크린샷: `evidence/screenshots/01_ssh_root_denied.png`)

### 1-4. 왜?

<!-- 포트 변경과 root 차단이 기본 보안인 이유, 포트 변경의 한계, drop-in 을 쓴 이유 (3~5문장) -->

증거: `evidence/01_ssh.txt`

---

## 2. 방화벽(UFW)

### 2-1. 수행 명령

```bash
<!-- ufw default ..., ufw allow ..., ufw enable -->
```

### 2-2. 확인 결과

```
<!-- sudo ufw status verbose -->
```

| 외부 테스트 (`nc -vz`) | 결과 |
|---|---|
| 20022 | <!-- succeeded --> |
| 15034 (앱 실행 중) | <!-- succeeded --> |
| 22 | <!-- timeout --> |

### 2-3. 왜?

<!-- 화이트리스트 정책, stateful, 설정 확인 vs 실측 검증 (3~5문장) -->

증거: `evidence/02_firewall.txt`

---

## 3. 계정 / 그룹

### 3-1. 수행 명령

```bash
<!-- groupadd, useradd, usermod -aG -->
```

### 3-2. 확인 결과

```
<!-- id agent-admin / id agent-dev / id agent-test -->
<!-- getent group agent-common agent-core -->
```

| 계정 | 역할 | agent-common | agent-core |
|---|---|---|---|
| agent-admin | 운영/관리, cron 실행자 | ● | ● |
| agent-dev | 개발/운영, monitor.sh 작성자 | ● | ● |
| agent-test | QA/테스트 | ● | – |

증거: `evidence/03_accounts.txt`

---

## 4. 디렉토리 구조 및 권한(ACL)

### 4-1. 설계

| 경로 | 소유자:그룹 | 권한 | ACL | 의도 |
|---|---|---|---|---|
| `/home/agent-admin` | agent-admin:agent-admin | 750 | `g:agent-common:--x` | <!-- --> |
| `$AGENT_HOME` | agent-admin:agent-common | 2750 | – | <!-- --> |
| `$AGENT_HOME/upload_files` | agent-admin:agent-common | 2770 | default `g::rwx,o::---` | <!-- --> |
| `$AGENT_HOME/api_keys` | agent-admin:agent-core | 2770 | default `g::rwx,o::---` | <!-- --> |
| `$AGENT_HOME/bin` | agent-dev:agent-core | 2750 | – | <!-- --> |
| `/var/log/agent-app` | agent-admin:agent-core | 2770 | default `g::rwx,o::---` | <!-- --> |

### 4-2. 수행 명령

```bash
<!-- mkdir / chown / chmod / setfacl -->
```

### 4-3. 확인 결과

```
<!-- sudo ls -la $AGENT_HOME, ls -ld /var/log/agent-app -->
<!-- sudo getfacl -p ... (핵심 부분) -->
```

권한 테스트 결과:

```
<!-- guide/04 2-5 권한 매트릭스 또는 evidence/04_dirs_acl.txt 의 PASS 표 -->
```

### 4-4. 왜?

<!-- 공유/보안 디렉토리 분리 이유, setgid·default ACL 을 쓴 이유 (3~5문장) -->

증거: `evidence/04_dirs_acl.txt`

---

## 5. 애플리케이션 실행 환경

### 5-1. 환경 변수

`/etc/agent-app/agent.env` (root:agent-common 640), `/etc/profile.d/agent-app.sh` 로 로그인 셸에 로드
```
<!-- 최종 agent.env 내용 -->
```

### 5-2. 키 파일

```
<!-- ls -l $AGENT_HOME/api_keys  (내용은 기재하지 않음) -->
```

### 5-3. ⚠️ 명세 편차(Deviation)

| 항목 | 명세(PDF) | 실제 앱 요구 | 근거(에러 메시지) | 조치 |
|---|---|---|---|---|
| `AGENT_KEY_PATH` | `$AGENT_HOME/api_keys/t_secret.key` | <!-- --> | <!-- 예: Key Path Mismatch. Expected: ... --> | <!-- --> |
| 키 파일명 | `t_secret.key` | <!-- --> | <!-- 예: Missing File: secret.key ... --> | <!-- --> |
| 프로세스명 | `agent_app.py` | `agent-app-linux` (15자 절단, PyInstaller 부모/자식 2개) | `ps -C agent-app-linux` | monitor.sh 에서 `pgrep -n -x agent-app-linux` |

<!-- 편차가 없었다면(케이스 A) "명세대로 5단계 통과, 편차 없음" 으로 기록 -->

### 5-4. 실행 및 결과

```
<!-- evidence/05_app_boot.txt 에서 Boot Sequence ~ Agent READY 부분 -->
```

```
<!-- sudo ss -tlnp 'sport = :15034' -->
<!-- ps -o user,pid,ppid,ni,cmd -C agent-app-linux -->
<!-- /proc/<PID>/environ 의 AGENT_* -->
```

### 5-5. 왜?

<!-- 환경 변수로 실행 환경을 고정하는 이유, 검증 방법, root 실행 금지 이유 (3~5문장) -->

증거: `evidence/05_env_app.txt`, `evidence/05_app_boot.txt`

---

## 6. monitor.sh

### 6-1. 파일 정보

```
<!-- ls -l $AGENT_HOME/bin/monitor.sh  →  -rwxr-x--- agent-dev agent-core -->
```

소스코드: `scripts/monitor.sh` (제출물 ②)

### 6-2. 설계 요약

| 기능 | 구현 방법 | 실패 시 |
|---|---|---|
| 프로세스 확인 | <!-- --> | exit 1 |
| 포트 확인 | <!-- --> | exit 1 |
| 방화벽 점검 | <!-- sudoers 최소 권한 --> | [WARNING], 계속 |
| CPU | <!-- --> | – |
| MEM | <!-- --> | – |
| DISK | <!-- --> | – |
| 임계값 | CPU>20, MEM>10, DISK>80 | [WARNING], 계속 |
| 로그 | `[YYYY-MM-DD HH:MM:SS] PID:.. CPU:..% MEM:..% DISK_USED:..%` | – |
| 용량 관리 | <!-- 10MB 초과 시 회전, 최대 10개 --> | – |

sudoers (`/etc/sudoers.d/agent-monitor`):
```
<!-- 내용 -->
```

### 6-3. 실행 결과

정상 실행:
```
<!-- evidence/06_monitor_run.txt -->
```

Health Check 실패(앱 중지 시):
```
<!-- [FAIL] + exit=1 -->
```

방화벽 비활성 시:
```
<!-- [WARNING] Firewall ... + exit=0 -->
```

로그 회전 테스트:
```
<!-- ls /var/log/agent-app 결과 (10개 이하) -->
```

### 6-4. monitor.log 누적 기록

```
<!-- tail -n 10 /var/log/agent-app/monitor.log -->
```

### 6-5. 왜?

<!-- 수집→기록→추적 흐름, exit 1 과 경고의 구분 이유, 로그 형식 설계 이유 (3~5문장) -->

증거: `evidence/06_monitor_run.txt`, `evidence/07_monitor_log.txt`

---

## 7. cron 자동 실행

### 7-1. 등록 내용

```
<!-- sudo crontab -u agent-admin -l -->
```

### 7-2. 자동 실행 확인

```
<!-- 시각 + wc -l (전) -->
<!-- 시각 + wc -l (1~2분 후) -->
<!-- tail -n 3 monitor.log -->
<!-- journalctl -t CRON ... (agent-admin CMD 줄) -->
```

### 7-3. 왜?

<!-- cron 환경의 특징과 대응, 출력 처리 방식, 로그 보존 정책이 필요한 이유 (3~5문장) -->

증거: `evidence/08_cron.txt`

---

## 8. (보너스) report.sh / log_retention.sh

```
<!-- report.sh 결과 -->
```

```
<!-- log_retention.sh dry-run / 실행 결과, 예외 처리 결과 -->
```

증거: `evidence/09_bonus.txt`

---

## 9. 현업 관점 회고 (서비스 로봇 온디바이스)

<!-- guide/field 문서를 읽고, 이번 미션 설정을 실제 현장 로봇에 적용한다면 무엇을 더/다르게 하겠는가? 3가지 이상 -->

1. <!-- 예: SSH 를 VPN 인터페이스에서만 허용하고 공개키 인증만 허용 -->
2. <!-- -->
3. <!-- -->

---

## 10. 필수 증거 체크리스트

| 미션 필수 증거 | 본문 | 증거 파일 | 확인 |
|---|---|---|---|
| SSH 포트 변경(20022) 및 Root 원격 접속 차단 | §1 | `01_ssh.txt` | ☐ |
| 방화벽 활성화 및 20022/tcp, 15034/tcp 만 허용 | §2 | `02_firewall.txt` | ☐ |
| 계정/그룹 생성 | §3 | `03_accounts.txt` | ☐ |
| 디렉토리 구조 및 권한(ACL 포함) | §4 | `04_dirs_acl.txt` | ☐ |
| 앱 Boot Sequence 5단계 [OK] 및 Agent READY | §5 | `05_app_boot.txt`, `05_env_app.txt` | ☐ |
| monitor.sh 실행 결과(프로세스/포트/리소스/경고) | §6 | `06_monitor_run.txt` | ☐ |
| monitor.log 누적 기록(최근 라인) | §6-4 | `07_monitor_log.txt` | ☐ |
| crontab 매분 실행 등록 및 자동 실행(1분 후 로그 증가) | §7 | `08_cron.txt` | ☐ |
